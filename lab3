--2. Вернитесь к заданиям из лабораторной работы № 2 (вторая часть заданий) и реализуйте их с помощью функций высшего порядка. Постарайтесь полностью исключить из определений функций явный проход по списку.

transform :: [Int] -> [Int]
transform list = map (\s -> if s > 0 then -1 * s else 1 * s) list -- map применяет функцию сразу ко всем элементам списка, чтобы не было явного прохода по списку

main :: IO ()
main = do
    let list = [-2, 0, 456, 2, -56, -9, 32]
    print(list)
    print(transform list)
